<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Acceder al almacenamiento externo -->
    <external-path
        name="external_files"
        path="." />

    <!-- Acceder a la carpeta de caché interna -->
    <cache-path
        name="cache_files"
        path="." />
</paths>
